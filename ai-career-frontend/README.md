AI Career Path Recommender - Frontend

Setup (local):
1. cd ai-career-frontend
2. npm install
3. npm run dev

Notes:
- Configure API base by setting REACT_APP_API_URL in your environment or adapt axios calls in source.
- Uses Vite + Tailwind + Framer Motion + Recharts.
